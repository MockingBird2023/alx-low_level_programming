#include "main.h"
/**
 * print_alphabet - print all alphabet in lowercase
 *
 * Return: always 0;
 */

void print_alphabet(void)
{
	char i;

	for (letter = 'a'; letter <= 'z'; i++)
		_putchar(i);
	_putchar('\n');
}
